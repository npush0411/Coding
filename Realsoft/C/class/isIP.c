#include <stdio.h>

int isIP(char k[])
{
    int i = 0, p_cnt = 0, no = 0;
    while(k[i] != '\0')
    {
        if(k[i] == '.')
        {
            p_cnt++;
            if(no > 255 && )
        }
        if(k[i] >= '0' && k[i] <= '9')

    }
}