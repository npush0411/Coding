#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, n, *ptr;
    printf("\nEnter No of Inputs : ");
    scanf("%d", &n);
    ptr = (int *)malloc(n*sizeof(int));
    for(i = 0; i < n; i++)
}
/*
    
*/